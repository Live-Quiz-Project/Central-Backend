package response

import (
	res "github.com/Live-Quiz-Project/Backend/internal/response/v1"
	"github.com/gin-gonic/gin"
)

func ResponseRoutes(r *gin.RouterGroup, h *res.Handler) {
	// r.POST("/responses", h.CreateResponse)
	// r.GET("/responses", h.Response)
	// r.GET("/responses/:id", h.ResponseByID)
	// r.PUT("/responses/:id", h.UpdateResponse)
	// r.DELETE("/responses/:id", h.DeleteResponse)
}
